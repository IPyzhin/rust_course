fn main() {
    /*
    Fahrenheit to Celsius
    (let fahrenheit: f64 = 78.8;
    let celsius = (fahrenheit - 32.0) * 5.0 / 9.0;
    println!("Temperature in Celsius: {celsius}");
    */
    let a = 10;
    let b = 2;
    println!("Sum: {}", a + b);
    println!("Difference: {}", a - b);
    println!("Product: {}", a * b);
    println!("Quotient: {}", a / b);
}